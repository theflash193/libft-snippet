<snippet>
	<content><![CDATA[
		int ${1:i};
		int ${2:j}j;

		i = 0;
		while (i < ${3:k})
		{
			${1:j} = 0;
			while (j < ${4:l})
			{
				j++;
			}
			i++;
		}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>iter_tableau</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
